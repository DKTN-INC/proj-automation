# Discord Bot Configuration
DISCORD_BOT_TOKEN=your_discord_token

# OpenAI (needed for /ask, planning, unit-test stubs, etc.)
OPENAI_API_KEY=your_openai_key

# Optional: GitHub (for PR helpers)
GITHUB_TOKEN=your_github_token

# Optional: PDF notifications via webhook (we’ll wire this in PR B)
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...

# Optional: faster slash command sync to a specific server (wired in PR B)
DISCORD_GUILD_ID=123456789012345678